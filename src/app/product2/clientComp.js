"use client"
import React from 'react'

const ClientComp = () => {
  return (
    <div>
            <button onClick={()=>alert("this is client comp")}>Click me</button>
    </div>
  )
}

export default ClientComp